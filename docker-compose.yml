version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "9000:9000"
  frontend:
    build: ./frontend
    ports:
      - "8000:8000"
    environment:
      ENDPOINT: http://localhost:9000/api